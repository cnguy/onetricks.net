{
  "name": "OneTricks",
  "version": "1.0.0",
  "main": "index.js",
  "repository": "https://github.com/cnguy/OneTricks",
  "author": "Chau <ChauTNguyen96@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.10.2"
  },
  "scripts": {
    "lint-client": "prettier --no-semi --single-quote --list-different --trailing-comma all --tab-width 4 --write \"./client/src/**/*.js\"",
    "lint-api": "prettier --no-semi --single-quote --list-different --trailing-comma all --tab-width 4 --write \"./api/{,!(node_modules)/**/}*.js\"",
    "lint-stats": "prettier --no-semi --single-quote --list-different --trailing-comma all --tab-width 4 --write \"./stats/{,!(node_modules)/**/}*.js\"",
    "lint": "yarn lint-client; yarn lint-api; yarn lint-stats"
  },
  "dependencies": {}
}
